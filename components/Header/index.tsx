import { Root, Logo } from './styles'
import { Container } from '../../lib/helpers'

export default () => (
  <Root>
    <Container>
      <Logo src="/static/logo.svg" />
    </Container>
  </Root>
)
